// image_handle: Handle<Image>
// duration: Duration

// Emits a temporary dialog bubble in the UI layer that stays over the head of
// the entity that made it. Action completes immediately, even while the effect
// continues.
